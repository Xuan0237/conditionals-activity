int score = 78; // You can change this value to test different scores

// Use the ternary operator to evaluate if the score is 50 or above
string result = (score >= 50) ? "Passed" : "Failed";

Console.WriteLine(result);
